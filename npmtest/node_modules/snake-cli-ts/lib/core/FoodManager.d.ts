import SnakeGame from './SnakeGame';
import Point from '../helpers/Point';
import Food from './Food';
export default class FoodManager {
    game: SnakeGame;
    foods: Food[];
    constructor(game: SnakeGame);
    find(x: number, y: number): Food | undefined;
    findIndex(x: number, y: number): number;
    remove(x: number, y: number): Food;
    add(): void;
    getRandomPoint(exceptions?: Point[]): Point;
    reset(): void;
}
